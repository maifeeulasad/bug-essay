{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pnpm build",
            "type": "pnpm",
            "script": "pnpm run build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}